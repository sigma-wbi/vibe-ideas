# Epic Context — 공유 분석

> 에픽: {{에픽명}}
> 생성일: {{DATE}}
> 마지막 갱신: {{DATE}}

> 이 문서는 에픽 전체 Phase가 공유하는 분석 결과를 담는다.
> Phase별 spec에서 `_context.md` 참조로 중복을 방지한다.

---

## 아키텍처 개요

### 현재 아키텍처

```
{{현재 시스템 구조 다이어그램 (텍스트)}}
```

### 목표 아키텍처

```
{{에픽 완료 후 시스템 구조 다이어그램 (텍스트)}}
```

### 변경 포인트

| # | 컴포넌트 | 현재 | 목표 | 변경 유형 |
|---|---------|------|------|----------|
| 1 | {{컴포넌트}} | {{현재 상태}} | {{목표 상태}} | 신규/수정/삭제 |

---

## 데이터 플로우

### 주요 데이터 흐름

```
{{데이터 흐름 다이어그램 (텍스트)}}
예시:
Client → API Gateway → AuthService → DB
                     → CacheLayer → Redis
```

### API 계약 (변경 대상)

| 엔드포인트 | 메서드 | 변경 내용 | 영향 Phase |
|-----------|--------|----------|-----------|
| {{/api/v1/...}} | {{GET/POST/...}} | {{변경 설명}} | P{{N}} |

### DB 스키마 변경 (해당 시)

| 테이블 | 변경 유형 | 컬럼 | Phase |
|--------|----------|------|-------|
| {{테이블명}} | 추가/수정/삭제 | {{컬럼 상세}} | P{{N}} |

---

## 코드 테이블

### 핵심 파일 맵

| # | 파일 | 역할 | 관련 Phase | 의존 관계 |
|---|------|------|-----------|----------|
| 1 | {{파일경로}} | {{역할 설명}} | P1, P2 | {{의존하는 파일}} |
| 2 | {{파일경로}} | {{역할 설명}} | P2 | {{의존하는 파일}} |

### 의존성 그래프

```
project_query(path="{{진입점}}", direction="both", depth=3)
```

| 소스 | 방향 | 대상 | 관계 유형 |
|------|------|------|----------|
| {{파일A}} | → | {{파일B}} | import |
| {{파일B}} | → | {{파일C}} | 호출 |

---

## 외부 의존성

| 의존성 | 버전 | 용도 | 리스크 |
|--------|------|------|--------|
| {{라이브러리/서비스}} | {{버전}} | {{용도}} | {{리스크}} |

---

## 관련 메모리

```
memory_search(query="{{에픽 도메인}}", limit=10)
knowledge_query(query="{{기술 스택}}", type="convention", limit=5)
```

| # | 유형 | 내용 요약 | score | 적용 |
|---|------|----------|-------|------|
| 1 | {{gotcha/pattern/decision}} | {{요약}} | {{0.XX}} | 필수/참조 |

---

> 이 문서는 Phase 진행에 따라 지속적으로 갱신된다.
> 갱신 시 `마지막 갱신` 날짜를 업데이트한다.
